import LibraryPage from "./LibraryPage"; import { useLibrary } from "../context/LibraryContext"; export default function FavoritesPage(){const{favorites}=useLibrary();return <LibraryPage eyebrow="Your collection" title="Favorite movies" text="The films you never want to forget." items={favorites} emptyTitle="No favorites yet" emptyText="Tap the heart on any movie to save it here."/>}
