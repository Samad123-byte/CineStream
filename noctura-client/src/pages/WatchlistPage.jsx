import LibraryPage from "./LibraryPage"; import { useLibrary } from "../context/LibraryContext"; export default function WatchlistPage(){const{watchlist}=useLibrary();return <LibraryPage eyebrow="Up next" title="My watchlist" text="Everything you are planning to watch." items={watchlist} emptyTitle="Your watchlist is empty" emptyText="Add movies to build your next movie night."/>}
